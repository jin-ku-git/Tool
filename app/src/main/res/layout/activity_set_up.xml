<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="com.youwu.tool.ui.set_up.SetUpViewModel" />

        <variable
            name="viewModel"
            type="SetUpViewModel" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:binding="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#202020"
        android:orientation="vertical">

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="网络访问"
            binding:onClickCommand="@{viewModel.netWorkClick}" />


        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="权限申请"
            binding:onClickCommand="@{viewModel.permissionsClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="全局异常捕获"
            binding:onClickCommand="@{viewModel.exceptionClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="文件下载"
            binding:onClickCommand="@{viewModel.fileDownLoadClick}" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="大"
            binding:onClickCommand="@{viewModel.largeClick}" />
    </LinearLayout>
</layout>